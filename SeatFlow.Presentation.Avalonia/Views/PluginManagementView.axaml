<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lang="using:SeatFlow.Presentation.Avalonia.Lang"
             xmlns:vm="using:SeatFlow.Presentation.Avalonia.ViewModels"
             xmlns:app="using:SeatFlow.Application.Interfaces"
             xmlns:conv="using:SeatFlow.Presentation.Avalonia.Converters"
             xmlns:fic="using:FluentIcons.Avalonia"
             xmlns:ficEnum="clr-namespace:FluentIcons.Common;assembly=FluentIcons.Common"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SeatFlow.Presentation.Avalonia.Views.PluginManagementView"
             x:DataType="vm:PluginManagementViewModel">

  <Design.DataContext>
    <vm:PluginManagementViewModel />
  </Design.DataContext>

  <UserControl.Resources>
    <conv:FilePathToBitmapConverter x:Key="IconConverter" />
  </UserControl.Resources>

  <DockPanel Classes="pageMargin">
    <!-- 顶部标题 -->
    <Border DockPanel.Dock="Top" Classes="toolbarTop">
      <DockPanel>
        <TextBlock DockPanel.Dock="Left" Text="{x:Static lang:Resources.Plugin_Title}" Classes="pageTitle"
                   VerticalAlignment="Center" />
        <Button DockPanel.Dock="Right" Command="{Binding RefreshPluginsCommand}"
                AutomationProperties.Name="{x:Static lang:Resources.Strategy_Plugin}"
                HorizontalContentAlignment="Center" Padding="12,6">
          <StackPanel Orientation="Horizontal" Spacing="6">
            <fic:FluentIcon Icon="{x:Static ficEnum:Icon.ArrowSync}" FontSize="16" />
            <TextBlock Text="{x:Static lang:Resources.Common_Refresh}" Classes="body" />
          </StackPanel>
        </Button>
      </DockPanel>
    </Border>

    <!-- 底部状态栏 -->
    <Border DockPanel.Dock="Bottom" Margin="0,8,0,0">
      <TextBlock Text="{Binding StatusMessage}" Classes="bodySmall"
                 Opacity="0.7" />
    </Border>

    <!-- 主内容区 -->
    <Grid ColumnDefinitions="280,*" ColumnSpacing="16">
      <!-- ═══ 左侧：插件列表 ═══ -->
      <Border Grid.Column="0" Background="{DynamicResource CardBackgroundBrush}"
              CornerRadius="8" Padding="0"
              BoxShadow="{DynamicResource CardShadowSmall}">
        <DockPanel>
          <Border DockPanel.Dock="Top" Padding="14,12">
            <TextBlock Text="{Binding PluginCountDisplay}"
                       Classes="bodyStrong" />
          </Border>

          <!-- 空列表提示 -->
          <TextBlock Text="{x:Static lang:Resources.Strategy_Plugin}"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Classes="body" Opacity="0.5"
                     IsVisible="{Binding EmptyHintVisible}" />

          <ListBox x:Name="PluginListBox" ItemsSource="{Binding Plugins}"
                   SelectedItem="{Binding SelectedPlugin, Mode=TwoWay}"
                   IsVisible="{Binding HasPlugins}"
                   Background="Transparent"
                   BorderThickness="0"
                   ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
              <DataTemplate x:DataType="app:PluginDisplayInfo">
                <Border Padding="12,10" Margin="4,2" CornerRadius="6"
                        Background="Transparent">
                  <Grid ColumnDefinitions="40,*" ColumnSpacing="10">
                    <!-- 图标 -->
                    <Border Grid.Column="0" Width="36" Height="36" CornerRadius="6"
                            Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                            ClipToBounds="True">
                      <Image Source="{Binding IconPath, Converter={StaticResource IconConverter}}"
                             Stretch="Uniform" />
                    </Border>
                    <!-- 信息 -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                      <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold"
                                 TextTrimming="CharacterEllipsis" />
                      <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="{Binding Version, StringFormat='v{0}'}" Classes="small"
                                   Opacity="0.6" />
                        <Border CornerRadius="3" Padding="4,1"
                                Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
                          <TextBlock Text="{Binding LoadKind}" Classes="micro" Opacity="0.7" />
                        </Border>
                      </StackPanel>
                    </StackPanel>
                  </Grid>
                </Border>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </DockPanel>
      </Border>

      <!-- ═══ 右侧：详情面板 ═══ -->
      <Border Grid.Column="1" Background="{DynamicResource CardBackgroundBrush}"
              CornerRadius="8" Padding="20"
              BoxShadow="{DynamicResource CardShadowSmall}"
              IsVisible="{Binding HasPluginSelected}">
        <ScrollViewer>
          <StackPanel Spacing="16">
            <!-- 头部信息 -->
            <Grid ColumnDefinitions="64,*" ColumnSpacing="14">
              <Border Grid.Column="0" Width="64" Height="64" CornerRadius="12"
                      Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                      ClipToBounds="True">
                <Image Source="{Binding SelectedPlugin.IconPath, Converter={StaticResource IconConverter}}"
                       Stretch="Uniform" />
              </Border>
              <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                <TextBlock Text="{Binding SelectedPlugin.Name}" Classes="pageTitle" />
                <TextBlock Text="{Binding SelectedPluginVersionDisplay}"
                           Classes="bodySmall" Opacity="0.6" />
                <TextBlock Text="{Binding SelectedPluginAuthorDisplay}"
                           Classes="bodySmall" Opacity="0.6"
                           IsVisible="{Binding SelectedPlugin.Author.Length, Converter={x:Static conv:BoolConverters.TrueToVisible}}" />
              </StackPanel>
            </Grid>

            <!-- 描述 -->
            <Border Padding="12,10" CornerRadius="6"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
              <TextBlock Text="{Binding SelectedPlugin.Description}" Classes="bodySmall"
                         TextWrapping="Wrap" Opacity="0.8" />
            </Border>

            <!-- 优先级 -->
            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Text="{x:Static lang:Resources.Strategy_PriorityLabel}" Classes="bodySmall" VerticalAlignment="Center" />
              <TextBlock Text="{Binding SelectedPlugin.Priority}" Classes="bodySmall"
                         FontWeight="SemiBold" VerticalAlignment="Center" />
            </StackPanel>

            <!-- 启用/禁用 ToggleSwitch -->
            <Border Padding="14,10" CornerRadius="6"
                    Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
              <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="{x:Static lang:Resources.Strategy_Plugin}" Classes="body"
                           VerticalAlignment="Center" />
                <ToggleSwitch x:Name="PluginEnabledSwitch" DockPanel.Dock="Right"
                              IsChecked="{Binding SelectedPlugin.IsEnabled, Mode=OneWay}"
                              OffContent="{x:Static lang:Resources.Plugin_Disabled}" OnContent="{x:Static lang:Resources.Strategy_EnableLabel}"
                              Command="{Binding ToggleEnabledCommand}" />
              </DockPanel>
            </Border>

            <Separator />

            <!-- 脚本编辑器（仅脚本插件） -->
            <StackPanel Spacing="8" IsVisible="{Binding ScriptEditorVisible}">
              <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="{x:Static lang:Resources.Plugin_ScriptEdit}" FontSize="15"
                           FontWeight="SemiBold" VerticalAlignment="Center" />
                <Button DockPanel.Dock="Right" Command="{Binding SaveScriptCommand}"
                        IsEnabled="{Binding IsScriptDirty}"
                        Padding="10,6" HorizontalContentAlignment="Center">
                  <StackPanel Orientation="Horizontal" Spacing="4">
                    <fic:FluentIcon Icon="{x:Static ficEnum:Icon.Save}" FontSize="14" />
                    <TextBlock Text="{x:Static lang:Resources.Plugin_SaveScript}" Classes="bodySmall" />
                  </StackPanel>
                </Button>
              </DockPanel>
              <Border CornerRadius="6" Padding="2"
                      Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
                <TextBox Text="{Binding ScriptEditorText, Mode=TwoWay}"
                         AcceptsReturn="True"
                         FontFamily="Cascadia Code,Consolas,monospace"
                         FontSize="13" MinHeight="240"
                         TextWrapping="NoWrap"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto" />
              </Border>
            </StackPanel>

            <!-- 配置编辑器（所有插件） -->
            <StackPanel Spacing="8" IsVisible="{Binding ConfigEditorVisible}">
              <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="{x:Static lang:Resources.Plugin_ConfigJSON}" FontSize="15"
                           FontWeight="SemiBold" VerticalAlignment="Center" />
                <Button DockPanel.Dock="Right" Command="{Binding SaveConfigCommand}"
                        IsEnabled="{Binding IsConfigDirty}"
                        Padding="10,6" HorizontalContentAlignment="Center">
                  <StackPanel Orientation="Horizontal" Spacing="4">
                    <fic:FluentIcon Icon="{x:Static ficEnum:Icon.Save}" FontSize="14" />
                    <TextBlock Text="{x:Static lang:Resources.Plugin_SaveConfig}" Classes="bodySmall" />
                  </StackPanel>
                </Button>
              </DockPanel>
              <Border CornerRadius="6" Padding="2"
                      Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
                <TextBox Text="{Binding ConfigEditorText, Mode=TwoWay}"
                         AcceptsReturn="True"
                         FontFamily="Cascadia Code,Consolas,monospace"
                         FontSize="13" MinHeight="200"
                         TextWrapping="NoWrap"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto" />
              </Border>
            </StackPanel>

          </StackPanel>
        </ScrollViewer>
      </Border>

      <!-- 未选中时提示 -->
      <Panel Grid.Column="1"
             IsVisible="{Binding HasPluginSelected, Converter={x:Static conv:BoolConverters.Negate}}">
        <Border Background="{DynamicResource CardBackgroundBrush}"
                CornerRadius="8" Padding="20"
                BoxShadow="{DynamicResource CardShadowSmall}">
          <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
            <fic:FluentIcon Icon="{x:Static ficEnum:Icon.PuzzlePiece}" FontSize="36"
                            Opacity="0.3" HorizontalAlignment="Center" />
            <TextBlock Text="{x:Static lang:Resources.Plugin_SelectHint}" Classes="bodyLarge"
                       Opacity="0.5" HorizontalAlignment="Center" />
          </StackPanel>
        </Border>
      </Panel>
    </Grid>
  </DockPanel>
</UserControl>
